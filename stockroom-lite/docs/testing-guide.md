# Testing Guide

## Backend
- Unit + API test runner: `npm test` in `backend/`
- Current test coverage includes health endpoint scaffold.

## Frontend
- Angular test runner: `npm test` in `frontend/`

## Validation Strategy
- Expand with role authorization tests.
- Add service-level tests for stock-in and stock-out transaction integrity.
